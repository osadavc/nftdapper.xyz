import type { NextPage } from "next";

import Header from "../components/Common/Header";

const Home: NextPage = () => {
  return (
    <div className="font-inter">
      <Header />
    </div>
  );
};

export default Home;
